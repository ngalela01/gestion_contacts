{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
{% for categorie in categories %}
    

<div class="col-sm-12 col-md-12 col-lg-4 col-xl-3  p-3" >
        <div class="card mb-3 d-flex justify-content-center">
            <h4 class="card-title text-center">{{categorie.libelle}} </h4>
            <img style="height: auto; max-width: 200px; display: block;" src="{{categorie.image}}" class=" mx-auto my-3" alt="...">
  
            
        
                   <div class="card-body">
                        <a href="{{path('categorie',{'id':categorie.id})}}" class="btn btn-primary text-center ">Détail </a>
                    </div>
        </div>
    </div>

{% endfor %}
{% endblock %}
